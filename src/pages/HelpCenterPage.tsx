import { DynamicModulePage } from './DynamicModulePage'

// Kept as a named export for the existing /help-center route
export function HelpCenterPage() {
  return <DynamicModulePage id="help-center" />
}
